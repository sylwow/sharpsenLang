public function void main() {
	first x;

	x.str = 90;
	//number tt = inc();
	number tt = x.increment();

	trace(toString(tt));
}

class first {
	number str;

	function number increment() {
		return 2*this.str;
	}
}

function number inc(first& f) {
	return ++f.str;
}

class sec {
	first str;
}

class myClass {
	sec field;
}