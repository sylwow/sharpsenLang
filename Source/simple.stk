public function void main() {
	first x;

	x.str = 90;
	//number tt = inc();
	string tt = inc(&x);

	trace(toString(tt));
}

class first {
	number str;

	function number increment() {
		number g = 10;
		g++;
		return g;
	}
}

function number inc(first& f) {
	return ++f.str;
}

class sec {
	first str;
}

class myClass {
	sec field;
}